### File selection and rendering rules

# Source-only license texts are included by LICENSE.jinja and must not be
# copied into generated projects.
_exclude:
  - .license-texts

# Copier's update algorithm should manage every generated file. In particular,
# policy, workflow, and lock files should continue to receive template updates.
_skip_if_exists: []
