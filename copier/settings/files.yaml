### File selection and rendering rules

# With _subdirectory set to a real path, default exclude is [].
# Declared here explicitly for clarity.
_exclude: []

# Files to recreate on update if missing but never overwrite if present.
_skip_if_exists: []  # TODO
