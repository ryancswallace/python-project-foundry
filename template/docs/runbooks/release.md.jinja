# Release

Prepare release metadata with:

```bash
{{ package_name | upper }}_RELEASE_VERSION=0.2.0 make prepare-release
```

Validate locally with:

```bash
make check
```

Create a release tag after the release pull request merges:

```bash
{{ package_name | upper }}_RELEASE_VERSION=0.2.0 make release-tag
```
