# Development

Use uv for all Python dependency management.

```bash
make ready
```

Run the full local validation suite before submitting changes:

```bash
make check
```

The package source lives in `src/{{ package_name }}/`, tests live in `tests/`,
and documentation lives in `docs/`.
